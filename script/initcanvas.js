const
    initCanvas = function () {
        graphics.width = document.body.clientWidth;
        graphics.height = document.body.clientHeight;
        
        foreground.width = document.body.clientWidth;
        foreground.height = document.body.clientHeight;
        
        background.width = document.body.clientWidth;
        background.height = document.body.clientHeight;
    };
